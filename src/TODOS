Create generic NodeInfo objects and populate the info from wf.cfg
(DONE) makefile add header file dependencies
Add system tests mechanism
(DONE) (bug) Check why PANID is not coming into effect from config file
In NS3/AirlineManager add read queue mechanism
	read_from_q(use_MANAGER_ID, buf, buflen)
	{
		sender_info_t *sndinf = buf;
		read_from_q(sndinf->id, buf, buflen)
	}

	when stackline sends a message ... it first sends a message to senderID and then to MANAGER_ID with senderinfo.



(DONE) Create a separate template for NS3 and Castalia
NS3 source for creating nodes and the topology ... Castalia <TODO>
 
